# 数据库连接 URL
DATABASE_URL="postgresql://beeve:beeve_password@localhost:5432/beeve"

# 服务端口号
PORT=3000

# Better Auth 密钥（用于签名和加密）
BETTER_AUTH_SECRET="your-secret-key-here"

# Better Auth 服务 URL
BETTER_AUTH_URL="http://localhost:3000"

# CORS 允许的源（前端地址）
CORS_ORIGIN="http://localhost:5173"

# ==================== Docker Compose 相关配置 ====================
# 以下变量用于 docker-compose.yml 中的 PostgreSQL 容器配置
# 如果仅使用本地数据库（非 Docker），可忽略这些变量

# PostgreSQL 用户名
POSTGRES_USER=beeve

# PostgreSQL 密码
POSTGRES_PASSWORD=beeve_password

# PostgreSQL 数据库名
POSTGRES_DB=beeve

# PostgreSQL 端口映射（宿主机端口）
POSTGRES_PORT=5432

