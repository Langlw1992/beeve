---
title: Button
description: 按钮组件 - 用于触发操作或事件
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import PropsTable from '../../../components/PropsTable.astro'
import ComponentPreview from '../../../components/ComponentPreview.astro'
import { ButtonVariants, ButtonSizes, ButtonStates, ButtonWithIcons } from '../../../components/demos/ButtonDemo'

Button 组件是最基础的交互元素，用于触发操作或事件。

## 导入

```tsx
import { Button } from '@beeve/ui'
```

## 变体 Variants

Button 支持多种视觉变体，适用于不同的使用场景。

<ComponentPreview>
  <ButtonVariants client:load />
</ComponentPreview>

```tsx
<Button variant="primary">Primary</Button>
<Button variant="secondary">Secondary</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="destructive">Destructive</Button>
<Button variant="link">Link</Button>
```

### 变体说明

| 变体 | 用途 |
|------|------|
| `primary` | 主要操作，页面上最重要的 CTA |
| `secondary` | 次要操作 |
| `outline` | 带边框的次要操作 |
| `ghost` | 最弱视觉强调，常用于工具栏 |
| `destructive` | 破坏性操作，如删除 |
| `link` | 外观像链接的按钮 |

## 尺寸 Sizes

<ComponentPreview>
  <ButtonSizes client:load />
</ComponentPreview>

```tsx
<Button size="sm">Small</Button>
<Button size="md">Medium</Button>
<Button size="lg">Large</Button>
<Button size="icon">🔔</Button>
```

## 状态 States

<ComponentPreview>
  <ButtonStates client:load />
</ComponentPreview>

```tsx
<Button>Normal</Button>
<Button disabled>Disabled</Button>
```

## 带图标 With Icons

<ComponentPreview>
  <ButtonWithIcons client:load />
</ComponentPreview>

```tsx
<Button>
  <PlusIcon />
  Add Item
</Button>
<Button variant="outline">
  <UploadIcon />
  Upload
</Button>
<Button variant="secondary">
  Settings
  <ChevronRightIcon />
</Button>
```

## API

<PropsTable
  props={[
    {
      name: 'variant',
      type: "'primary' | 'secondary' | 'outline' | 'ghost' | 'destructive' | 'link'",
      default: "'primary'",
      description: '按钮的视觉变体'
    },
    {
      name: 'size',
      type: "'sm' | 'md' | 'lg' | 'icon'",
      default: "'md'",
      description: '按钮尺寸'
    },
    {
      name: 'disabled',
      type: 'boolean',
      default: 'false',
      description: '是否禁用按钮'
    },
    {
      name: 'type',
      type: "'button' | 'submit' | 'reset'",
      default: "'button'",
      description: '按钮类型'
    },
    {
      name: 'class',
      type: 'string',
      description: '自定义 CSS 类名'
    },
    {
      name: 'onClick',
      type: '(e: MouseEvent) => void',
      description: '点击事件处理函数'
    },
    {
      name: 'children',
      type: 'JSX.Element',
      required: true,
      description: '按钮内容'
    }
  ]}
/>

## 无障碍

- 使用原生 `<button>` 元素确保键盘可访问
- `disabled` 状态会自动添加 `aria-disabled` 属性
- 确保按钮有清晰的文本标签或 `aria-label`

## 主题集成

Button 组件自动使用主题系统的 CSS 变量：

- `--primary` / `--primary-foreground` - 主按钮颜色
- `--secondary` / `--secondary-foreground` - 次要按钮颜色
- `--destructive` - 危险操作颜色
- `--radius` - 圆角大小
- `--ring` - 焦点环颜色

当主题设置变化时（如切换深色模式或更改主题色），按钮会自动更新样式。
