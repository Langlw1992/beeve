---
title: Button
description: 按钮组件 - 用于触发操作或事件
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import PropsTable from '../../../components/PropsTable.astro'

Button 组件是最基础的交互元素，用于触发操作或事件。

## 导入

```tsx
import { Button } from '@beeve/ui'
```

## 变体 Variants

Button 支持多种视觉变体，适用于不同的使用场景。

<div class="not-content my-6 p-6 border border-slate-200 dark:border-slate-700 rounded-lg bg-slate-50 dark:bg-slate-900">
  <div class="flex flex-wrap gap-3 items-center">
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors h-10 px-4 text-sm bg-violet-500 text-white hover:bg-violet-600">Primary</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors h-10 px-4 text-sm bg-slate-100 text-slate-900 hover:bg-slate-200 dark:bg-slate-800 dark:text-slate-100">Secondary</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors h-10 px-4 text-sm border border-slate-300 bg-transparent hover:bg-slate-100 dark:border-slate-700 dark:hover:bg-slate-800">Outline</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors h-10 px-4 text-sm bg-transparent hover:bg-slate-100 dark:hover:bg-slate-800">Ghost</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors h-10 px-4 text-sm bg-red-500 text-white hover:bg-red-600">Destructive</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors h-10 px-4 text-sm text-violet-500 underline-offset-4 hover:underline bg-transparent">Link</button>
  </div>
</div>

```tsx
<Button variant="primary">Primary</Button>
<Button variant="secondary">Secondary</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="destructive">Destructive</Button>
<Button variant="link">Link</Button>
```

### 变体说明

| 变体 | 用途 |
|------|------|
| `primary` | 主要操作，页面上最重要的 CTA |
| `secondary` | 次要操作 |
| `outline` | 带边框的次要操作 |
| `ghost` | 最弱视觉强调，常用于工具栏 |
| `destructive` | 破坏性操作，如删除 |
| `link` | 外观像链接的按钮 |

## 尺寸 Sizes

<div class="not-content my-6 p-6 border border-slate-200 dark:border-slate-700 rounded-lg bg-slate-50 dark:bg-slate-900">
  <div class="flex flex-wrap gap-3 items-center">
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors bg-violet-500 text-white hover:bg-violet-600 h-8 px-3 text-sm">Small</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors bg-violet-500 text-white hover:bg-violet-600 h-10 px-4 text-sm">Medium</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors bg-violet-500 text-white hover:bg-violet-600 h-12 px-6 text-base">Large</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors bg-violet-500 text-white hover:bg-violet-600 h-10 w-10">🔔</button>
  </div>
</div>

```tsx
<Button size="sm">Small</Button>
<Button size="md">Medium</Button>
<Button size="lg">Large</Button>
<Button size="icon">🔔</Button>
```

## 状态 States

<div class="not-content my-6 p-6 border border-slate-200 dark:border-slate-700 rounded-lg bg-slate-50 dark:bg-slate-900">
  <div class="flex flex-wrap gap-3 items-center">
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors h-10 px-4 text-sm bg-violet-500 text-white hover:bg-violet-600">Normal</button>
    <button class="inline-flex items-center justify-center gap-2 font-medium rounded-lg transition-colors h-10 px-4 text-sm bg-violet-500 text-white opacity-50 cursor-not-allowed" disabled>Disabled</button>
  </div>
</div>

```tsx
<Button>Normal</Button>
<Button disabled>Disabled</Button>
```

## API

<PropsTable
  props={[
    {
      name: 'variant',
      type: "'primary' | 'secondary' | 'outline' | 'ghost' | 'destructive' | 'link'",
      default: "'primary'",
      description: '按钮的视觉变体'
    },
    {
      name: 'size',
      type: "'sm' | 'md' | 'lg' | 'icon'",
      default: "'md'",
      description: '按钮尺寸'
    },
    {
      name: 'disabled',
      type: 'boolean',
      default: 'false',
      description: '是否禁用按钮'
    },
    {
      name: 'class',
      type: 'string',
      description: '自定义 CSS 类名'
    },
    {
      name: 'onClick',
      type: '() => void',
      description: '点击事件处理函数'
    },
    {
      name: 'children',
      type: 'JSX.Element',
      required: true,
      description: '按钮内容'
    }
  ]}
/>

## 无障碍

- 使用原生 `<button>` 元素确保键盘可访问
- `disabled` 状态会自动添加 `aria-disabled` 属性
- 确保按钮有清晰的文本标签或 `aria-label`

## 源码

```tsx
import { splitProps, type Component, type JSX } from 'solid-js'
import { tv, type VariantProps } from 'tailwind-variants'

const buttonVariants = tv({
  base: [
    'inline-flex items-center justify-center gap-2',
    'font-medium rounded-lg',
    'transition-colors duration-200',
    'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2',
    'disabled:pointer-events-none disabled:opacity-50',
  ],
  variants: {
    variant: {
      primary: 'bg-primary-500 text-white hover:bg-primary-600',
      secondary: 'bg-slate-100 text-slate-900 hover:bg-slate-200 dark:bg-slate-800 dark:text-slate-100',
      outline: 'border border-slate-300 bg-transparent hover:bg-slate-100 dark:border-slate-700',
      ghost: 'bg-transparent hover:bg-slate-100 dark:hover:bg-slate-800',
      destructive: 'bg-red-500 text-white hover:bg-red-600',
      link: 'text-primary-500 underline-offset-4 hover:underline',
    },
    size: {
      sm: 'h-8 px-3 text-sm',
      md: 'h-10 px-4 text-sm',
      lg: 'h-12 px-6 text-base',
      icon: 'h-10 w-10',
    },
  },
  defaultVariants: {
    variant: 'primary',
    size: 'md',
  },
})

export type ButtonVariants = VariantProps<typeof buttonVariants>

export interface ButtonProps extends ButtonVariants {
  class?: string
  children?: JSX.Element
  disabled?: boolean
  type?: 'button' | 'submit' | 'reset'
  onClick?: (e: MouseEvent) => void
}

export const Button: Component<ButtonProps> = (props) => {
  const [local, variants, rest] = splitProps(
    props, 
    ['class', 'children'], 
    ['variant', 'size']
  )

  return (
    <button class={buttonVariants({ ...variants, class: local.class })} {...rest}>
      {local.children}
    </button>
  )
}
```
