---
title: Select 选择器
description: 下拉选择器组件
---

import { SelectBasic, SelectSizes, SelectWithGroups, SelectDisabled, SelectWithLabel } from '../../../components/demos/SelectDemo'

Select 组件用于从一组选项中选择一个值，基于 Zag.js 实现。

## 基础用法

<SelectBasic client:only="solid-js" />

```tsx
import { Select } from '@beeve/ui'
import { createSignal } from 'solid-js'

const [value, setValue] = createSignal('')

<Select
  value={value()}
  onChange={(v) => setValue(v as string)}
  placeholder="请选择"
  options={[
    { value: 'apple', label: '苹果' },
    { value: 'banana', label: '香蕉' },
    { value: 'orange', label: '橙子' },
  ]}
/>
```

## 选择器尺寸

<SelectSizes client:only="solid-js" />

```tsx
<Select size="sm" placeholder="小尺寸" options={options} />
<Select size="md" placeholder="中尺寸" options={options} />
<Select size="lg" placeholder="大尺寸" options={options} />
```

## 分组选项

<SelectWithGroups client:only="solid-js" />

```tsx
<Select
  placeholder="选择水果"
  options={[
    {
      label: '热带水果',
      options: [
        { value: 'mango', label: '芒果' },
        { value: 'pineapple', label: '菠萝' },
      ],
    },
    {
      label: '温带水果',
      options: [
        { value: 'apple', label: '苹果' },
        { value: 'pear', label: '梨' },
      ],
    },
  ]}
/>
```

## 禁用状态

<SelectDisabled client:only="solid-js" />

```tsx
<Select placeholder="禁用选择器" disabled options={options} />

<Select
  placeholder="禁用部分选项"
  options={[
    { value: '1', label: '可选项' },
    { value: '2', label: '禁用项', disabled: true },
  ]}
/>
```

## 带标签选择器

<SelectWithLabel client:only="solid-js" />

```tsx
import { Label, Select } from '@beeve/ui'

<div class="space-y-2">
  <Label for="country" required>国家/地区</Label>
  <Select id="country" placeholder="请选择" options={countries} />
</div>
```

## API

### SelectProps

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `value` | `string` | - | 当前值（受控） |
| `defaultValue` | `string` | - | 默认值（非受控） |
| `onChange` | `(value: string, option: SelectOption) => void` | - | 值变化回调 |
| `options` | `SelectOption[]` | **必填** | 选项数据 |
| `placeholder` | `string` | - | 占位文本 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | 选择器尺寸 |
| `disabled` | `boolean` | `false` | 禁用状态 |
| `id` | `string` | - | 元素 ID |
| `name` | `string` | - | 表单字段名 |

### SelectOption

| 属性 | 类型 | 说明 |
|------|------|------|
| `value` | `string` | 选项值 |
| `label` | `string` | 显示文本 |
| `disabled` | `boolean` | 是否禁用 |
| `options` | `SelectOption[]` | 分组子选项 |
